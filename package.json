{
  "name": "deepgram-proxy",
  "version": "1.0.0",
  "description": "Proxy WebSocket léger pour Deepgram",
  "main": "server.js",
  "scripts": {
    "start": "node --optimize_for_size --gc_interval=100 server.js",
    "start:dev": "node server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "ws": "^8.13.0",
    "@deepgram/sdk": "^2.4.0"
  }
}